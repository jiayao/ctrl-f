"""jev-digest: guided reading paths for text-based PDFs.

Selects and labels the PDF's original words with TypeSafe Jev,
then writes an annotated PDF copy plus a JSON manifest.
"""

from jev_digest.constants import __version__

__all__ = ["__version__"]
